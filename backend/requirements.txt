# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
httpx==0.25.1

# Ovi dependencies (from Ovi repository)
# Note: Install Ovi separately from https://github.com/character-ai/Ovi
# or use: pip install git+https://github.com/character-ai/Ovi.git
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.30.0
diffusers>=0.21.0
accelerate>=0.20.0

# Gemini API
# Using HTTP client directly since VEO API may not be in Python SDK yet
# httpx is already included above

# Utilities
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0

# GPU support
nvidia-ml-py3>=7.352.0  # For GPU detection

